import java.awt.*;

/**
 * A GUI class for the theme of the application.
 */
public class Theme {
    static final Color RED = new Color(220, 53, 69);
    static final Color RED_DARK = new Color(200, 40, 55);
    static final Color YELLOW = new Color(255, 193, 7);

    // Dark mode greys
    static final Color BG    = new Color(40, 44, 52);   // window background
    static final Color CARD  = new Color(58, 63, 72);   // panels/cards/header

    // Text
    static final Color TEXT   = new Color(245, 245, 245); // main text (white-ish)
    static final Color SUBTLE = new Color(200, 200, 200); // secondary text

    static final Font BUTTON_FONT = new Font(Font.SANS_SERIF, Font.BOLD, 13);
    static final int RADIUS = 18;
}
